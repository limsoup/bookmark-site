//clone new_bookmark, then change the first one
// $('#new_bookmark').before($('.new_bookmark').clone())
// $('.new_bookmark').attr('class','created_bookmark');	//this should only select the first one

$('#add_bookmark_js').hide();
$('#new_bookmark').before($("<%= j render('bookmark_fields_new_js') %>").find('#pull').children());
$('#new_bookmark').prev().prev().show().on('dblclick', function(){
		$(this).hide();
		$(this).prev('.show_bookmark').show();
	}).find('input').data('params',$('#new_bookmark').prev('input[type=hidden]').serialize()).prev().hide().on('dblclick', function(){
		$(this).hide();
		$(this).next('.edit_bookmark').show();
	});
//$('.created_bookmark').append($("<%= j render('bookmark_fields_new_js') %>").find('.gen_bookmark_field').children().first());
// $('#created_bookmark a.remove_bookmark').hide();
// unsaved_new = $('#new_bookmark');
// unsaved_new.before(unsaved_new.clone()).hide();
// unsaved_new= unsaved_new.unwrap();
// unsaved_new.children('input,label').removeData('disabled');


// $('#created_bookmark').on('hitenter?', function() {		//change #new_bookmark to something else maybe
// 		$('#add_bookmark_js').show();		//enable link adding bookmarks
// 		// $('#created_bookmark a.remove_bookmark').show();	//enable link for deleting bookmarks
// 		//update
// 		//modify delete link after to add id
// 		//$(this).unwrap(); //remove div element since it's not the 

// 		//do all the things that bookmarkSetup does once
// 		//add any event handlers associated with all bookmarks
// 		$(this).bookmarkSetup();
// });

/*
jQuery.fn.bookmarkSetup = function (){
	on-input-change
			change background
	on-enter
			change background back
			save
}
*/